#include <fcntl.h>
#include <sys/stat.h>
#include <unistd.h>
#include <string.h>

int main() {
  int fd = open("tmp", O_RDWR | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR);

  char *msg = "PLEASE WORK\n";
  write(fd, msg, strlen(msg));
  sleep(30);
}
