#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();
  if (pid != 0) {
    char* args[] = {"/bin/ls","-a","-l","-h", NULL};
    execv("/bin/ls", args);
  } else {
    execl("/bin/ls", "/bin/ls", "/home/cmpt201", "-a", "-l", "-h", NULL);
  }
}
