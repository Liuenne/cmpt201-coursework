#include <unistd.h>
#include <sys/stat.h>
#include <fcntl.h>


int main() {
  int fd = open("tmp", O_RDWR | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR);

  char* msg = "PLEASE WORK\n";
  write(fd, msg, sizeof(msg));
  sleep(30);
}
