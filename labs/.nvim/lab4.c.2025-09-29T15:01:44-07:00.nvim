#define _POSIX_C_SOURCE 200809L
#include <unistd.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>

struct header {
  uint64_t size;
  struct header *next;
};

#define BLK_SIZE 128

int main() { 
  void *base = sbrk(256);

  if (base == (void *)-1) {
    perror("sbrk failed");
    return 1;
  }
}
