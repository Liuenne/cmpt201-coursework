#define _POSIX_C_SOURCE 200809L
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  char *input = NULL;
  size_t size = 0;
  char *token = NULL;

  printf("Enter programs to run.\n");
  ssize_t cmd = getline(&input, &size, stdin);
  




  printf("%d",cmd);
}
